import styled from "styled-components";

const HomeLayout = styled.main`
    display: flex;
    flex-direction: column;
    /* align-items: center; */
    margin-top: 100px;
    max-width: 500px;
`;

export { HomeLayout };
